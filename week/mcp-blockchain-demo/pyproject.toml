[project]
name = "mcp-blockchain-demo"
version = "0.1.0"
description = "Weather MCP demo and blockchain/coin MCP final project example"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "starlette>=0.40.0",
    "uvicorn>=0.30.0",
]

[tool.uv]
package = false
