# Week 6 제출양식

> 이 파일을 복사해 본인 파일로 만들고 모든 `<...>` 자리를 채운 뒤 제출하세요.
> Etherscan 링크는 접두사 `https://sepolia.etherscan.io/` 포함 전체 URL.

## 기본 정보

- 이름: `<이름>`
- 학번: `<학번>`
- 제출일: `<YYYY-MM-DD>`

## 배포 주소

| 항목 | 주소 | Etherscan 링크 |
|---|---|---|
| 내 ERC20 (또는 HNL 재사용 시 HNL 주소) | `<TOKEN_ADDRESS>` | `<https://sepolia.etherscan.io/address/...>` |
| StakingContract (Sepolia) | `<STAKING_ADDRESS>` | `<...>` |
| 본인 지갑 주소 | `<STUDENT_WALLET>` | `<...>` |

사용한 스테이킹 버전: `[ ] Fast  [ ] Daily  [ ] 본인 구현`

## 트랜잭션 해시

| 동작 | Tx Hash | Etherscan 링크 |
|---|---|---|
| stake | `<STAKE_TX_HASH>` | `<https://sepolia.etherscan.io/tx/...>` |
| claimReward | `<CLAIM_TX_HASH>` | `<...>` |
| (선택) unstake | `<UNSTAKE_TX_HASH>` | `<...>` |

## 소스 검증

- [ ] Etherscan `Contract` 탭에 ✅ Verified 뱃지 표시됨
- 사용한 컴파일러 버전: `<v0.8.x+commit.xxxxxxx>`
- Optimizer: `[ ] On (runs=200)`

## 보상 수식 설명 (한 줄 이상)

예시: "내 `_updateReward`는 amount × rate × Δt / 1e18 으로 누적 보상을 계산하고 lastUpdate를 갱신한다.
Daily 버전은 rate = 1e18/86400 이므로 스테이크 1 토큰이 86400초 뒤 약 1 토큰을 준다."

```
<여기에 작성>
```

## 어려웠던 점 / 배운 점 / 소감

```
<한 문단 이상 자유 서술>
```

## 추가 사항 (선택)

- DApp 실행 경험 여부: `[ ] 예  [ ] 아니오`
- 발견한 버그·개선 제안:
```
<없으면 비워두기>
```
