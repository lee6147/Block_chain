# 04. 실습: 지갑 간 송금과 트랜잭션 추적

> [!info] 학습 목표
> 1. GIWA Sepolia 테스트넷에서 실제 ETH 송금을 수행한다
> 2. 트랜잭션 해시를 통해 Nonce, Gas, Signature를 **직접 눈으로** 확인한다
> 3. 이론에서 배운 개념이 실제 트랜잭션에 어떻게 반영되는지 체험한다

**사전 학습**: [[02-중급-Nonce-Gas-Signature]]

> [!quote] 이 실습의 핵심
> "블록체인은 추상적인 이론이 아니다 — 모든 것이 **숫자로 기록되고, 누구나 검증할 수 있다**."

---

## 실습 전체 흐름

```mermaid
flowchart LR
    subgraph PREP["🔧 사전 준비"]
        A["MetaMask 설정"] --> B["Private Key 내보내기"]
        B --> C[".env 파일 설정"]
    end

    subgraph STEP2["📤 2단계: 송금"]
        D["자기 자신에게\n테스트 송금"] --> E["짝꿍과\n주소 교환"]
        E --> F["상대방에게\n ETH 송금"]
    end

    subgraph STEP3["🔍 3단계: 추적"]
        G["Tx Hash로\n트랜잭션 조회"] --> H["Nonce·Gas·Sig\n확인"]
        H --> I["개념 매핑\n토론"]
    end

    PREP --> STEP2 --> STEP3

    style PREP fill:#1a1a2e,stroke:#16213e,color:#e0e0e0
    style STEP2 fill:#0f3460,stroke:#16213e,color:#e0e0e0
    style STEP3 fill:#533483,stroke:#16213e,color:#e0e0e0
```

---

## 1. 사전 준비

### 1-1. MetaMask에 GIWA Sepolia 네트워크 추가

| 항목 | 값 |
|------|-----|
| **네트워크 이름** | GIWA Sepolia |
| **RPC URL** | `https://sepolia-rpc.giwa.io/` |
| **Chain ID** | `91342` |
| **통화 기호** | ETH |
| **Explorer URL** | `https://sepolia-explorer.giwa.io/` |

> [!tip] MetaMask 네트워크 추가 경로
> MetaMask → 설정(⚙️) → 네트워크 → **네트워크 추가** → 위 정보 입력

### 1-2. Private Key 내보내기 → `.env` 설정

```mermaid
flowchart LR
    A["MetaMask\n계정 클릭"] --> B["⋮ 메뉴\n계정 세부정보"]
    B --> C["비공개 키\n내보내기"]
    C --> D["비밀번호 입력\n→ 키 복사"]
    D --> E[".env 파일에\n붙여넣기"]

    style A fill:#f6851b,stroke:#e2761b,color:#fff
    style E fill:#2ecc71,stroke:#27ae60,color:#fff
```

```env
# .env 파일
PRIVATE_KEY=여기에_복사한_개인키_붙여넣기
```

> [!danger] Private Key 보안 경고
> - Private Key는 **절대 다른 사람에게 공유하지 않는다**
> - 짝꿍과 교환하는 것은 **지갑 주소(0x...)** 뿐이다
> - `.env` 파일은 `.gitignore`에 포함되어 Git에 올라가지 않는다
>
> | 공유 가능 | 공유 불가 |
> |-----------|-----------|
> | 지갑 주소 `0x...` | Private Key |
> | Tx Hash | .env 파일 |
> | 공개키 (Public Key) | 시드 구문 (12/24단어) |

---

## 2. 2단계: 지갑 간 송금

### 2-1. 실습 진행 순서

```mermaid
sequenceDiagram
    participant S as 🧑‍💻 나 (송신자)
    participant N as 🌐 GIWA 노드
    participant R as 👩‍💻 짝꿍 (수신자)

    Note over S: Step A: 먼저 자기 자신에게 테스트

    S->>N: node scripts/02-send-eth.js
    N-->>S: ✅ 자기 송금 성공 (Nonce 0 → 1)

    Note over S,R: Step B: 짝꿍과 주소 교환

    R-->>S: "내 주소는 0xABC...야"
    S-->>R: "내 주소는 0xDEF...야"

    Note over S,R: Step C: 상대방에게 송금

    S->>N: node scripts/02-send-eth.js 0xABC... 0.001
    N->>R: 0.001 ETH 전달
    N-->>S: ✅ Tx Hash: 0x789...

    Note over S: Step D: 결과 확인 (Nonce, Gas, 수수료)
```

---

### Step A: 자기 자신에게 테스트 송금

> [!example] 실행 명령어
> ```bash
> node scripts/02-send-eth.js
> ```
> 받는 주소를 생략하면 **자기 자신에게** 0.001 ETH를 보낸다.
> 스크립트가 정상 동작하는지 먼저 확인하는 용도이다.

**예상 출력:**

```
  [송금 전 상태]
  잔액      : 0.1 ETH
  현재 Nonce: 0
  → 이번 tx는 Nonce 0 으로 발행됩니다

  ⏳ 트랜잭션 전송 중...

  ✅ 트랜잭션 완료!

  [트랜잭션 영수증]
  블록 번호 : 12345
  상태      : 성공 ✅
  Gas Used  : 21000 (실제 사용량)
  Gas Price : 0.001 Gwei
  실제 수수료: 0.000000021 ETH

  [송금 후 상태]
  잔액      : 0.099999979 ETH
  현재 Nonce: 1 ← Nonce가 0 → 1 로 +1 증가!
```

> [!warning] 자기 송금인데 왜 잔액이 줄어들었을까?
> 보낸 금액(0.001 ETH)은 자기에게 다시 돌아오지만,
> **Gas 수수료**(0.000000021 ETH)는 네트워크에 지불한 것이므로 **돌아오지 않는다**.
>
> ```
> 잔액 변화 = 송금액(자기에게 복귀) − Gas 수수료(네트워크에 지불)
>           = 0                     − 0.000000021
>           = −0.000000021 ETH
> ```

---

### Step B: 짝꿍과 주소 교환

```mermaid
flowchart LR
    subgraph 나
        MY_ADDR["내 지갑 주소\n0xDEF..."]
    end

    subgraph 짝꿍
        PAIR_ADDR["짝꿍 지갑 주소\n0xABC..."]
    end

    MY_ADDR -- "주소만 교환\n(Private Key ❌)" --> PAIR_ADDR
    PAIR_ADDR -- "주소만 교환\n(Private Key ❌)" --> MY_ADDR

    style MY_ADDR fill:#3498db,stroke:#2980b9,color:#fff
    style PAIR_ADDR fill:#e74c3c,stroke:#c0392b,color:#fff
```

> [!tip] 주소 교환 방법
> - MetaMask에서 자기 주소를 **복사** → 카카오톡/슬랙으로 전달
> - 또는 QR 코드로 교환

---

### Step C: 상대방에게 송금

> [!example] 실행 명령어
> ```bash
> node scripts/02-send-eth.js 0x짝꿍주소 0.001
> ```

---

### Step D: 출력 결과에서 확인해야 할 핵심 항목

| 항목 | 예시 값 | 의미 | 관련 개념 |
|------|---------|------|-----------|
| **송금 전 Nonce** | `1` | 이전까지 보낸 트랜잭션 수 | [[02-중급-Nonce-Gas-Signature#1. Nonce 심화]] |
| **송금 후 Nonce** | `2` | +1 증가 확인 | 리플레이 공격 방지 |
| **Gas Used** | `21000` | 단순 전송의 고정 Gas | 연산 비용 단위 |
| **Gas Price** | `0.001 Gwei` | Gas 1단위당 가격 | EIP-1559 |
| **실제 수수료** | `0.000000021 ETH` | gasUsed × gasPrice | 스팸 방지 |
| **Tx Hash** | `0x789...` | 트랜잭션 고유 식별자 | **3단계에서 사용** |

> [!success] Tx Hash를 반드시 복사해두세요!
> 3단계 트랜잭션 추적에서 이 해시를 사용합니다.

---

### 토론 질문

> [!question] 학생들에게 던질 질문
>
> **Q1. Nonce가 왜 1에서 2로 올랐을까?**
> → 같은 트랜잭션이 두 번 처리되는 것을 방지하기 위해서이다 (리플레이 공격 방지)
>
> **Q2. 0.001 ETH를 보냈는데 잔액이 0.001보다 더 줄었다. 왜?**
> → 보낸 금액 + Gas 수수료가 함께 차감되기 때문이다
>
> **Q3. Gas가 없으면 어떤 문제가 생길까?**
> → 악의적 사용자가 무한으로 트랜잭션을 보내 네트워크를 마비시킬 수 있다 (DoS 공격)
>
> **Q4. 자기 자신에게 송금하면 돈이 왜 줄어들까?**
> → 금액은 돌아오지만, Gas 수수료는 네트워크에 지불되므로 환불되지 않는다

---

## 3. 3단계: 트랜잭션 추적

### 3-1. 추적 방법 (2가지)

```mermaid
flowchart TD
    TX_HASH["Tx Hash\n(2단계에서 복사한 것)"]

    subgraph 방법1["방법 1: CLI 스크립트"]
        CMD["node scripts/03-track-tx.js 0x해시"]
        RESULT1["터미널에서\nNonce·Gas·Signature 확인"]
    end

    subgraph 방법2["방법 2: GIWA Explorer (웹)"]
        URL["sepolia-explorer.giwa.io\n/tx/0x해시"]
        RESULT2["브라우저에서\n시각적으로 확인"]
    end

    TX_HASH --> CMD --> RESULT1
    TX_HASH --> URL --> RESULT2

    style TX_HASH fill:#f39c12,stroke:#e67e22,color:#fff
    style 방법1 fill:#1a1a2e,stroke:#16213e,color:#e0e0e0
    style 방법2 fill:#1a1a2e,stroke:#16213e,color:#e0e0e0
```

> [!example] CLI로 추적
> ```bash
> node scripts/03-track-tx.js 0x2단계에서_복사한_해시
> ```

> [!example] 웹 Explorer로 추적
> 브라우저에서 아래 주소에 접속한다:
> ```
> https://sepolia-explorer.giwa.io/tx/0x2단계에서_복사한_해시
> ```

---

### 3-2. 트랜잭션에서 확인할 수 있는 정보 전체 구조

```mermaid
mindmap
  root((트랜잭션))
    기본 정보
      Hash — 고유 식별자
      From — 보낸 주소
      To — 받는 주소
      Value — 전송 금액
    Nonce
      순번 — 몇 번째 거래인가
      중복 방지 — 리플레이 공격 차단
      순서 보장 — 앞 번호 먼저 처리
    Gas
      Gas Limit — 최대 허용량
      Gas Used — 실제 사용량
      Gas Price — 단위당 가격
      실제 수수료 — Used × Price
    Signature
      r — 타원곡선 x좌표
      s — 수학적 증명값
      v — 복구 식별자
```

---

### 3-3. 핵심 개념 심화 설명

#### 개념 1: Nonce — "이 트랜잭션이 몇 번째인가"

```mermaid
sequenceDiagram
    participant A as Alice (Nonce: 0)
    participant Net as GIWA 네트워크
    participant Evil as 🦹 공격자

    A->>Net: TX #0 (Nonce=0): Bob에게 1 ETH
    Net-->>A: ✅ 처리 완료 (Nonce → 1)

    Evil->>Net: TX 복사본 (Nonce=0): Bob에게 1 ETH
    Net-->>Evil: ❌ 거부! Nonce 0은 이미 사용됨

    Note over Net: Nonce 덕분에<br/>같은 트랜잭션의<br/>재전송(리플레이)이<br/>불가능하다
```

> [!abstract] 핵심 정리
> | 속성 | 설명 |
> |------|------|
> | **시작값** | 0 (해당 주소의 첫 번째 트랜잭션) |
> | **증가 규칙** | 트랜잭션 성공 시 +1 |
> | **범위** | 계정별 독립 (Alice와 Bob의 Nonce는 무관) |
> | **핵심 역할** | 리플레이 공격 방지 + 트랜잭션 순서 보장 |

> [!warning] Nonce가 꼬이면 일어나는 일
> ```
> 현재 Nonce = 3인 상태에서...
>
> Nonce 5인 트랜잭션을 먼저 보냄
> → ❌ pending (대기) 상태로 멈춤
> → Nonce 3, 4가 먼저 처리될 때까지 영원히 대기
>
> 해결: Nonce 3 → 4 → 5 순서대로 보내야 함
> ```

---

#### 개념 2: Gas — "트랜잭션 수수료"

```mermaid
flowchart TD
    subgraph 비유["택시 요금 비유"]
        METER["🚕 미터기 기본요금\n= baseFee\n(네트워크가 자동 결정)"]
        TIP["💰 팁\n= priorityFee\n(내가 선택)"]
        TOTAL_FARE["💳 총 요금\n= baseFee + priorityFee"]
        METER --> TOTAL_FARE
        TIP --> TOTAL_FARE
    end

    subgraph 실제["실제 Gas 계산"]
        BASE["baseFee\n(블록 혼잡도에 따라 변동)"]
        PRIORITY["priorityFee (tip)\n(빨리 처리하려면 높게)"]
        FORMULA["실제 수수료\n= gasUsed × (baseFee + tip)"]
        BASE --> FORMULA
        PRIORITY --> FORMULA
    end

    style 비유 fill:#1a1a2e,stroke:#f39c12,color:#e0e0e0
    style 실제 fill:#1a1a2e,stroke:#3498db,color:#e0e0e0
```

##### Gas Limit vs Gas Used

> [!important] 이 둘의 차이를 반드시 구분해야 한다

```mermaid
flowchart LR
    LIMIT["Gas Limit\n= 21,000\n(내가 설정한 상한선)"]
    EXEC["EVM 실행\n(트랜잭션 처리)"]
    USED["Gas Used\n= 21,000\n(실제 소모량)"]
    REFUND["환불\n= Limit - Used\n= 0"]

    LIMIT --> EXEC --> USED --> REFUND

    style LIMIT fill:#e74c3c,stroke:#c0392b,color:#fff
    style USED fill:#2ecc71,stroke:#27ae60,color:#fff
    style REFUND fill:#3498db,stroke:#2980b9,color:#fff
```

| 구분 | Gas Limit | Gas Used |
|------|-----------|----------|
| **정의** | 사용자가 허용하는 최대 Gas | 실제 소모된 Gas |
| **설정 주체** | 사용자 (또는 지갑이 자동 추정) | 네트워크가 실행 후 확정 |
| **단순 ETH 전송** | 21,000 | 21,000 (항상 동일) |
| **스마트 컨트랙트** | 예측 필요 (estimateGas) | 복잡도에 따라 달라짐 |
| **Limit < 필요량일 때** | 실행 도중 실패 (out of gas) | 쓴 만큼만 기록, **환불 없음** |

> [!danger] Gas Limit을 너무 낮게 잡으면?
> - 트랜잭션 실행 도중 Gas가 바닥남 → **즉시 실패 (revert)**
> - 그런데 **이미 쓴 Gas 비용은 환불되지 않는다**
> - 즉, 수수료만 날리고 트랜잭션은 실패하는 최악의 상황
>
> ```
> 예시:
> Gas Limit = 15,000 (너무 낮게 설정)
> 실제 필요 = 21,000
> → 15,000 지점에서 실행 중단
> → 15,000 Gas 비용 차감됨 (환불 없음)
> → 트랜잭션 상태: 실패 ❌
> ```

---

#### 개념 3: Signature (r, s, v) — "내가 보냈다는 증명"

> [!tip] 이 개념이 가장 어렵다. 비유부터 시작하자.

##### 비유: 위조 불가능한 도장

```mermaid
flowchart TD
    subgraph 현실["🏛️ 현실 세계"]
        SEAL["인감도장으로\n계약서에 날인"]
        VERIFY_REAL["등기소에서\n인감 대조 → 본인 확인"]
    end

    subgraph 블록체인["⛓️ 블록체인"]
        SIGN["Private Key로\n트랜잭션에 서명\n→ r, s, v 생성"]
        VERIFY_CHAIN["ecrecover로\n서명 → 주소 복원\n→ 본인 확인"]
    end

    SEAL -.->|"동일한 원리"| SIGN
    VERIFY_REAL -.->|"동일한 원리"| VERIFY_CHAIN

    style 현실 fill:#2c3e50,stroke:#34495e,color:#ecf0f1
    style 블록체인 fill:#1a1a2e,stroke:#16213e,color:#e0e0e0
```

##### 서명의 생성과 검증 과정

```mermaid
flowchart TD
    subgraph 생성["🔐 서명 생성 (송신자 측)"]
        direction TB
        TX["트랜잭션 데이터\n(to, value, nonce, gas...)"]
        HASH["Keccak-256 해시\n(데이터 → 32바이트 요약)"]
        ECDSA["ECDSA 서명 알고리즘\n+ Private Key"]
        RSV["서명값 {r, s, v}"]

        TX --> HASH --> ECDSA --> RSV
    end

    subgraph 검증["✅ 서명 검증 (노드 측)"]
        direction TB
        RECV["수신한 트랜잭션\n+ 서명 {r, s, v}"]
        ECRECOVER["ecrecover 함수\n(해시 + r, s, v)"]
        PUB["Public Key 복원\n→ 주소 변환"]
        MATCH{"복원된 주소\n==\nfrom 주소?"}
        OK["✅ 유효한 서명\n→ 트랜잭션 처리"]
        FAIL["❌ 서명 불일치\n→ 트랜잭션 거부"]

        RECV --> ECRECOVER --> PUB --> MATCH
        MATCH -- "일치" --> OK
        MATCH -- "불일치" --> FAIL
    end

    RSV --> RECV

    style 생성 fill:#0f3460,stroke:#16213e,color:#e0e0e0
    style 검증 fill:#533483,stroke:#16213e,color:#e0e0e0
    style OK fill:#2ecc71,stroke:#27ae60,color:#fff
    style FAIL fill:#e74c3c,stroke:#c0392b,color:#fff
```

##### r, s, v 각각의 의미

| 값 | 크기 | 비유 | 기술적 의미 |
|----|------|------|-------------|
| **r** | 32 bytes | 도장의 **모양** | 서명 과정에서 생성된 타원곡선 위의 점의 x좌표 |
| **s** | 32 bytes | 도장의 **잉크 패턴** | Private Key와 r을 조합해 계산한 수학적 증명값 |
| **v** | 1 byte | 도장을 **어느 방향으로 읽는지** 표시 | Public Key 복원 시 2개 후보 중 정답을 고르는 힌트 |

> [!abstract] 가장 핵심적인 포인트
>
> ```
> ┌──────────────────────────────────────────────────┐
> │  서명(r, s, v)만 있으면                            │
> │  → 누가 보냈는지 알 수 있다 (주소 복원 가능)          │
> │                                                    │
> │  하지만 서명(r, s, v)에서                           │
> │  → Private Key는 절대 알아낼 수 없다                 │
> │                                                    │
> │  이것이 타원곡선 암호(ECDSA)의 핵심 보안 속성이다     │
> └──────────────────────────────────────────────────┘
> ```

##### 왜 역방향 계산이 불가능한가? (색 섞기 비유)

```mermaid
flowchart LR
    subgraph 순방향["✅ 순방향 (쉬움)"]
        RED["🔴 빨강\n(Private Key)"]
        BLUE["🔵 파랑\n(랜덤값)"]
        PURPLE["🟣 보라\n(Public Key)"]
        RED --> PURPLE
        BLUE --> PURPLE
    end

    subgraph 역방향["❌ 역방향 (불가능)"]
        PURPLE2["🟣 보라\n(Public Key)"]
        Q1["🔴 빨강?\n🟠 주황?\n🟤 갈색?"]
        PURPLE2 --> Q1
    end

    style 순방향 fill:#2ecc71,stroke:#27ae60,color:#fff
    style 역방향 fill:#e74c3c,stroke:#c0392b,color:#fff
```

> [!info] 수학적 배경 (관심 있는 학생용)
> - 이더리움은 **secp256k1** 타원곡선을 사용한다
> - `Public Key = Private Key × G` (G는 고정된 생성점)
> - 곱셈(순방향)은 컴퓨터로 즉시 가능
> - 나눗셈(역방향, 이산 로그 문제)은 현재 기술로 **수십억 년** 소요
> - 양자 컴퓨터가 실용화되면 깨질 수 있다 → 이더리움도 양자 내성 암호 연구 중

---

## 4. 실습 결과물 예시

### CLI 스크립트 출력 전체 흐름

```
  ============================================
   3단계: 트랜잭션 추적
  ============================================

  [트랜잭션 기본 정보]
  Hash      : 0x789abc...
  From      : 0xDEF...  ← 보낸 사람 (나)
  To        : 0xABC...  ← 받는 사람 (짝꿍)
  Value     : 0.001 ETH
  Nonce     : 1         ← 두 번째 트랜잭션
  Gas Limit : 21000

  [서명 정보 (Private Key 소유 증명)]
  r : 0x3a8f...  ← 타원곡선 x좌표
  s : 0x7b2e...  ← 수학적 증명값
  v : 1          ← 복구 식별자

  [트랜잭션 영수증 (블록 포함 후)]
  상태      : 성공 ✅
  블록 번호 : 12345
  Gas Used  : 21000
  Gas Price : 0.001 Gwei
  실제 수수료: 0.000000021 ETH
```

### Explorer에서 확인하는 화면과 대응

```mermaid
flowchart TD
    subgraph EXPLORER["🌐 GIWA Explorer 화면 구성"]
        direction TB
        HEADER["Transaction Details"]

        BASIC["📋 기본 정보\nTx Hash · Status · Block"]
        ADDR["📍 주소 정보\nFrom · To"]
        VALUE["💰 금액 정보\nValue · Tx Fee"]
        GAS["⛽ Gas 정보\nGas Limit · Gas Used · Gas Price"]
        SIG["🔐 서명 정보\nr · s · v"]

        HEADER --> BASIC --> ADDR --> VALUE --> GAS --> SIG
    end

    subgraph CONCEPTS["📚 대응하는 개념"]
        direction TB
        C1["트랜잭션 식별"]
        C2["송수신자 확인"]
        C3["전송 금액 + 수수료"]
        C4["Nonce · Gas\n→ 순서 보장 · 스팸 방지"]
        C5["Signature\n→ 본인 인증"]
    end

    BASIC -.-> C1
    ADDR -.-> C2
    VALUE -.-> C3
    GAS -.-> C4
    SIG -.-> C5

    style EXPLORER fill:#1a1a2e,stroke:#f39c12,color:#e0e0e0
    style CONCEPTS fill:#1a1a2e,stroke:#3498db,color:#e0e0e0
```

---

## 5. 전체 개념 연결 — 트랜잭션의 생애주기

```mermaid
flowchart TD
    START["👤 사용자가\n송금 요청"]

    NONCE["1️⃣ Nonce 결정\n'이건 내 N번째 거래'"]
    GAS["2️⃣ Gas 설정\n'수수료는 이만큼 낼게'"]
    SIGN["3️⃣ 서명 생성\n'Private Key로 도장 찍기'"]
    SEND["4️⃣ 네트워크에 전송"]

    VERIFY["5️⃣ 노드가 검증"]
    V_SIG["서명 유효?\necrecover → 주소 일치?"]
    V_NONCE["Nonce 순서 맞음?"]
    V_BAL["잔액 충분?\nvalue + gas ≤ balance"]

    POOL["6️⃣ 트랜잭션 풀\n(대기열)에 추가"]
    BLOCK["7️⃣ 블록에 포함"]
    RECEIPT["8️⃣ 영수증 발행\n(gasUsed, status, logs)"]
    DONE["✅ 완료\nNonce +1 증가"]

    START --> NONCE --> GAS --> SIGN --> SEND --> VERIFY
    VERIFY --> V_SIG --> V_NONCE --> V_BAL
    V_BAL --> POOL --> BLOCK --> RECEIPT --> DONE

    V_SIG -- "❌ 실패" --> REJECT1["거부: 서명 불일치"]
    V_NONCE -- "❌ 실패" --> REJECT2["거부: Nonce 불일치"]
    V_BAL -- "❌ 실패" --> REJECT3["거부: 잔액 부족"]

    style START fill:#3498db,stroke:#2980b9,color:#fff
    style SIGN fill:#e74c3c,stroke:#c0392b,color:#fff
    style DONE fill:#2ecc71,stroke:#27ae60,color:#fff
    style REJECT1 fill:#95a5a6,stroke:#7f8c8d,color:#fff
    style REJECT2 fill:#95a5a6,stroke:#7f8c8d,color:#fff
    style REJECT3 fill:#95a5a6,stroke:#7f8c8d,color:#fff
```

---

## 6. 핵심 요약 — 한 장 정리

> [!summary] 3주차 핵심 개념 한눈에 보기
>
> | 개념 | 한 줄 정의 | 없으면 일어나는 일 | 비유 |
> |------|-----------|-------------------|------|
> | **Nonce** | 트랜잭션 순번 | 같은 거래가 무한 반복됨 (리플레이 공격) | 택배 송장 일련번호 |
> | **Gas** | 네트워크 사용 수수료 | 무한 스팸으로 네트워크 마비 (DoS 공격) | 택시 미터기 요금 |
> | **Signature** | Private Key로 만든 디지털 서명 | 누구나 남의 돈을 보낼 수 있음 | 위조 불가능한 인감도장 |
>
> ```
> 트랜잭션 = Nonce(순서) + Gas(수수료) + Signature(인증)
>          = "몇 번째"  + "얼마 낼지" + "내가 보냈다는 증명"
> ```

---

## 다음 단계

이 실습을 완료했다면, 서명 검증 실습으로 넘어간다:

> [!tip] 4단계: 서명 검증
> ```bash
> node scripts/04-verify-sig.js
> ```
> 임의의 메시지에 서명하고, 서명에서 주소를 복원하여 본인 인증을 직접 체험한다.

**이전 문서**: [[02-중급-Nonce-Gas-Signature]]
**다음 문서**: [[03-고급-L2아키텍처와-보안]]

---

#blockchain #GIWA #Sepolia #실습 #Nonce #Gas #Signature #트랜잭션
